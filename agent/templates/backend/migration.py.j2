from alembic import op
import sqlalchemy as sa

revision = "auto_{{ spec.feature.name }}"
down_revision = None
branch_labels = None
depends_on = None

def upgrade():
    op.create_table(
        "{{ spec.feature.model.table }}",
        sa.Column("id", sa.String(length=36), primary_key=True),
        sa.Column("name", sa.String(length=120), nullable=False),
        sa.Column("email", sa.String(length=255), nullable=False, unique=True, index=True),
        sa.Column("status", sa.String(length=32), nullable=False, default="active", index=True),
        sa.Column("created_at", sa.DateTime(), nullable=False),
        sa.Column("updated_at", sa.DateTime(), nullable=False),
    )
    op.create_index("ix_{{ spec.feature.model.table }}_status", "{{ spec.feature.model.table }}", ["status"], unique=False)

def downgrade():
    op.drop_index("ix_{{ spec.feature.model.table }}_status", table_name="{{ spec.feature.model.table }}")
    op.drop_table("{{ spec.feature.model.table }}")
