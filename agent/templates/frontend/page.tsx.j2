export default function {{ spec.feature.name|capitalize }}Page() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">{{ spec.feature.name|capitalize }}</h1>
      <p>Scaffold generated at {{ now }}</p>
    </div>
  )
}
